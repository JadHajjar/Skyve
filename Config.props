<Project>
	<PropertyGroup Condition="'$(Configuration)' == 'Stable'">
		<DefineConstants>$(DefineConstants);Stable</DefineConstants>
		<DebugType>none</DebugType>
		<Optimize>true</Optimize>
	</PropertyGroup>

	<PropertyGroup>
		<Company>T. D. W.</Company>
		<Copyright>@2023 MIT license</Copyright>
		<Product>Skyve App</Product>
	</PropertyGroup>

	<PropertyGroup Condition="'$(Configuration)' == 'Stable' OR '$(Configuration)' == 'Debug' OR '$(Configuration)' == 'Release'">
		<DefineConstants>CS1</DefineConstants>
	</PropertyGroup>
</Project>